# Gold Challenge

This stage extended the buggy project with computer vision using a HuskyLens module.

The Arduino code handled buggy control and camera input, while the Processing sketches were used to test and visualise the detected environment on a laptop.